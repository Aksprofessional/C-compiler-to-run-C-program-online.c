# C-compiler-to-run-C-program-online.c

// Online C compiler to run C program online
#include <stdio.h>
int fnn(float,int);
int main() {
    int n;
    n=fnn(7.2,2);
    printf("%d",n);
    return 0;
}

int fnn(float a,int b){
    return a*b;
}
